[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "analyse_exec_utils"
version = "0.0.2"
authors = [
  { name="Ganesh Nukala", email="ganesh@mobiux.in" },
]
description = "A small python package with utilities to analyse code performance"
readme = "README.md"
requires-python = ">=3.10"