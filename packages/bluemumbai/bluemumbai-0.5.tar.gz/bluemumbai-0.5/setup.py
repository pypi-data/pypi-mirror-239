from setuptools import setup

setup(
    name='bluemumbai',
    version='0.5',
    description='Print and copy greetings based on user input for bluemumbai',
    author='Your Name',
    packages=['bluemumbai'],
    install_requires=['pyperclip'],
)
