[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "custom_switch_case"
description = "A sample python package to implement switch case in python"
version = "0.0.6"
readme = "README.md"
requires-python = ">=3.7"
dependencies = []