[project]
name = "dog1"
version = "0.1.4"
description = ""
authors = [
    { name = "Meng Xiangzhuo", email = "aumo@foxmail.com" },
]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"

[project.license]
text = "MIT"

[project.urls]
repository = "https://github.com/xzmeng/dog1"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm.build]
includes = [
    "src",
]
