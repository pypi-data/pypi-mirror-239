Value mgmt_vid (.*)
Value vid (.*)
Value acs_ports (.*)
Value trk_ports (.*)
Value hyb_ports (.*)
Value brg_ports (.*)

Start
 ^vlan mode: 802.1Q vlan
 ^mgmt vlan:\s+${mgmt_vid}
 ^VLAN ${vid}:
 ^\s+Access Ports:\s+${acs_ports}
 ^\s+Trunk Ports:
 ^\s+Hybrid Ports:
 ^\s+Bridge Ports: -> Record