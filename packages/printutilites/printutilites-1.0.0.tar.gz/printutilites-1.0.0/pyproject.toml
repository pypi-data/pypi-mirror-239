[tool.poetry]
name = "printutilites"
version = "1.0.0"
description = "Print message repeat"
authors = ["daurenmuratov <daurenmuratov@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
